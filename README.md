#Homwork 2 -Robot FLeet Manager
Simulation of fleet of robotics: cleaning, cooking and mobile by assigning tasks with priority, threads and exceptions.
Ro compile:
bash conpile.sh
./fleet_app

I faces problems in:
- naming a header file mobile_robots and used mobile robot in all other includes which created a mess at the begining (cascaded errors).
- Accidently had an initializer Robot() inside {} instead of the way around (outside which got rejected bby the compiler giving that the Robot is abstract and cannot be instantiated directly.
- Forgot the password for github push , so I got a new token and saved it locally.

